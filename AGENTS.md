任何场景下，如果开发过程中使用了兜底或者 fallback 策略，都先向我确认！！禁止擅自做主添加了兜底逻辑！！

只使用一次的功能禁止抽出一个独立helper！直接inline实现！

严禁直接使用 `git clean`、`git reset --hard`、`git checkout -- .`、`git restore .` 或其他等价命令清除所有未缓存/未跟踪文件。只有在我明确逐字要求清理这些文件时才允许执行。
